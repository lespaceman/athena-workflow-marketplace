interface:
  display_name: "Linear"
  short_description: "Read and update Linear issues, projects, and delivery state"
  default_prompt: "Use Linear to inspect or update issue, project, and delivery state. Keep tracker updates concise and grounded in the current work."

dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear MCP tools for issue, project, document, attachment, and workflow-state operations"
