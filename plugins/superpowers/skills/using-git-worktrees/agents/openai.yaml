interface:
  display_name: "Using Git Worktrees"
  short_description: "Use when starting feature work that needs isolation from current workspace or before"
  default_prompt: "Apply the using-git-worktrees skill to the current task."
