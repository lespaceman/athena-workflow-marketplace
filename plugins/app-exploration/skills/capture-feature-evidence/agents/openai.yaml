interface:
  display_name: "Capture Feature Evidence"
  short_description: "Capture grounded evidence for one scoped feature area"
  default_prompt: "Capture evidence for this scoped feature area, stay within the assigned boundary, and write the structured exploration artifact with evidence, blockers, and unresolved questions."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to inspect the live product and capture evidence"
