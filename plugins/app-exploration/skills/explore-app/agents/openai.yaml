interface:
  display_name: "Explore App"
  short_description: "Capture grounded product evidence and blockers from a live app"
  default_prompt: "Explore this app, capture directly observed behavior for the target journey, and write an exploration report with evidence, blockers, and unresolved questions."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to inspect the live product and capture evidence"
