interface:
  display_name: "Explore App"
  short_description: "Deeply explore one scoped feature area and capture grounded evidence and blockers"
  default_prompt: "Deeply explore this scoped feature area, keep within the assigned sub-feature boundary, and write the structured exploration artifact with evidence, blockers, and unresolved questions."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to inspect the live product and capture evidence"
