interface:
  display_name: "Explore App"
  short_description: "Explore a live app and capture reusable testing evidence"
  default_prompt: "Explore this app, record grounded evidence for the target journey, and write a reusable exploration report for downstream planning and implementation."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to inspect the live product and capture evidence"
