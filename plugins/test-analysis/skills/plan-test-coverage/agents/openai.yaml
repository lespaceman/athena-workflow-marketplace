interface:
  display_name: "Plan Coverage Priorities"
  short_description: "Turn exploration evidence and optional charter context into final coverage priorities"
  default_prompt: "Review the available exploration evidence, reuse `exploratory-charter.md` when present for mission and risk context, and produce the final prioritized E2E coverage plan without writing specs or code."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Optional browser automation MCP used only for bounded evidence spot-checks when shared exploration artifacts are incomplete"
