interface:
  display_name: "Fix Flaky Robot Framework Tests"
  short_description: "Diagnose unstable Robot Framework tests and fix the root cause"
  default_prompt: "Diagnose this flaky Robot Framework test, reproduce the failure, and fix the root cause."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to reproduce and inspect unstable flows"
