interface:
  display_name: "Run Full Robot Framework Workflow"
  short_description: "Orchestrate the Robot execution layer using shared exploration and test-analysis artifacts"
  default_prompt: "Run the Robot Framework execution workflow for this feature: verify shared exploration and planning artifacts, then analyze, implement, review, and verify the .robot tests."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP available to shared exploration and selector verification flows"
