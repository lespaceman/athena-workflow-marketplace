interface:
  display_name: "Review Robot Framework Test Code"
  short_description: "Review Robot Framework test implementation for stability and correctness"
  default_prompt: "Review this Robot Framework test implementation before execution and flag quality issues."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to verify locators against the live site"
