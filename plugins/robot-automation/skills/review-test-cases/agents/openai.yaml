interface:
  display_name: "Review TC-ID Specs (Robot)"
  short_description: "Review TC-ID specs for gaps, duplication, and weak assertions before .robot implementation"
  default_prompt: "Review these TC-ID test case specifications before Robot Framework implementation and flag quality issues."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to spot-check observed behavior claims against live site"
