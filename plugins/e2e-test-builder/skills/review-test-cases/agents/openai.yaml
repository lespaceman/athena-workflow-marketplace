interface:
  display_name: "Review Test Cases"
  short_description: "Review TC-ID specs for gaps, duplication, and quality issues"
  default_prompt: "Review the test case specifications for completeness and quality before implementation."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to spot-check observed behavior claims against live site"
