interface:
  display_name: "Fix Flaky Tests"
  short_description: "Stabilize flaky or timing-sensitive Playwright tests"
  default_prompt: "Diagnose this flaky Playwright test and fix the root cause."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to reproduce and inspect unstable flows"
