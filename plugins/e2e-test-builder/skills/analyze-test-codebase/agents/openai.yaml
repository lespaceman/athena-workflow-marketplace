interface:
  display_name: "Analyze Existing Test Setup"
  short_description: "Inspect the existing Playwright setup, conventions, and test infrastructure"
  default_prompt: "Analyze this Playwright codebase and summarize the current setup before any test changes."
