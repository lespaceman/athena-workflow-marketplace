interface:
  display_name: "Add E2E Tests"
  short_description: "Run the full Playwright test-building workflow"
  default_prompt: "Add Playwright E2E tests for this feature end to end."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to explore the site and verify flows"
