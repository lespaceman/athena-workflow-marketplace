interface:
  display_name: "Run Full E2E Workflow"
  short_description: "Run the full workflow from discovery to passing Playwright tests"
  default_prompt: "Run the full E2E workflow for this feature and deliver passing Playwright tests."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to explore the site and verify flows"
