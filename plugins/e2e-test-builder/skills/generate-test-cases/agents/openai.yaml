interface:
  display_name: "Generate Test Cases"
  short_description: "Create TC-ID test specifications from a live web flow"
  default_prompt: "Explore this feature and generate structured TC-ID test cases."

dependencies:
  tools:
    - type: "mcp"
      value: "agent-web-interface"
      description: "Browser automation MCP used to inspect the live flow before writing specs"
