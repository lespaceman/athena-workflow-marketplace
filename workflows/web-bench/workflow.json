{
  "name": "web-bench",
  "description": "Benchmark runner for WebBench — executes one real-world browser task per session from the Halluminate/WebBench dataset, scores via LLM-as-judge, and produces evaluation reports with timing and token usage",
  "promptTemplate": "{input}",
  "systemPromptFile": "system_prompt.md",
  "loop": {
    "enabled": true,
    "completionMarker": "BENCH_COMPLETE",
    "blockedMarker": "BENCH_BLOCKED",
    "trackerPath": "web-bench-tracker.md",
    "maxIterations": 2500
  },
  "isolation": "minimal",
  "plugins": ["web-bench@lespaceman/athena-workflow-marketplace"]
}
