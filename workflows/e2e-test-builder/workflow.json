{
  "name": "e2e-test-builder",
  "description": "Multi-session workflow for adding Playwright E2E tests end to end: start with orchestration, analyze the existing setup, explore the live product in a browser, maintain a concise tracker with meaningful progress updates, plan coverage, generate and review specs, write and review tests, then execute and stabilize them",
  "promptTemplate": "{input}",
  "systemPromptFile": "system_prompt.md",
  "loop": {
    "enabled": true,
    "completionMarker": "E2E_COMPLETE",
    "blockedMarker": "E2E_BLOCKED",
    "trackerPath": "e2e-tracker.md",
    "maxIterations": 15
  },
  "isolation": "minimal",
  "plugins": [
    "agent-web-interface@lespaceman/athena-workflow-marketplace",
    "e2e-test-builder@lespaceman/athena-workflow-marketplace"
  ]
}
